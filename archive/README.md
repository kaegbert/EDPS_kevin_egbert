# Archive

Older site code kept for reference while learning with the simplified flexbox-only setup.

| File | What it was |
|------|-------------|
| `styles-pre-simplify.css` | Full stylesheet with CSS grid, mobile hamburger nav, reveal animations, page-specific classes |
| `script.js` | Mobile nav toggle, scroll reveal, contact form |
| `classes.html` | CSS class reference page |

The live site now uses only `styles.css` (flexbox shell + 5 classes) with inline styles on each page for learning.
